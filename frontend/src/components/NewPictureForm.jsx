import React, { useEffect } from "react";

const NewPictureForm = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return <h1>New Picture Form</h1>;
};

export default NewPictureForm;
