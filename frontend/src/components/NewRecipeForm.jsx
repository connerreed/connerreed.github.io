import React, { useEffect } from "react";

const NewRecipeForm = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return <h1>New Recipe Form</h1>;
};

export default NewRecipeForm;
